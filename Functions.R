

A1 = array(seq(0.2,1.0, 0.1), dim = c(3, 3))
A2 = array(seq(1.2,2.0, 0.1), dim = c(3, 3))

A1 
A2

A1 + A2
A2 - A1
A1 / A2
A1 * A2 #not matrix multiplication

A1 %*% A2 # matrix multiplication syntax

